import { WhatsAppButton } from "@/components/shared/WhatsAppButton";

/** Site-wide floating UI (WhatsApp, etc.) — rendered on every route and branch. */
export function GlobalChrome() {
  return <WhatsAppButton />;
}
